# Shopify CLI local files
.shopify/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node
node_modules/
npm-debug.log*
package-lock.json

# Local assets / tooling output
*.log
dist/
.tmp/

# Part 4 pixel-capture artifacts (26 MB — regenerate with scripts/px-check)
scripts/px-check/prototype/
scripts/px-check/target/

# Keep the pixel-harness lockfile for reproducible installs
!scripts/px-check/package-lock.json

# Dev-only credentials — never commit store passwords
*.env
.env.local
# Local-only submission email (contains the storefront password — send then delete)
notes/mail.txt

# python
__pycache__/
*.pyc
